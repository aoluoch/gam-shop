-- Seed file for GAM Shop
-- This file runs after migrations to set up initial data

-- Note: Admin users should be created through the Supabase dashboard or CLI
-- After creating an admin user via signup, run:
-- UPDATE public.profiles SET role = 'admin' WHERE id = 'your-admin-user-uuid';

-- Example: Create a test admin (uncomment and replace with actual UUID after signup)
-- UPDATE public.profiles SET role = 'admin' WHERE email = 'admin@example.com';
